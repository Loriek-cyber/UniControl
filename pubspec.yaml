name: uni_control
description: A premium new Flutter project.
publish_to: 'none'
version: 1.0.1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  google_fonts: ^6.1.0
  device_preview: ^1.3.1
  device_info_plus: ^9.1.2
  uuid: ^4.5.3
  # Add other dependencies here (e.g., flutter_riverpod, bloc, get_it, etc.)

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  # assets:
  #   - assets/images/
